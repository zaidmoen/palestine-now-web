# سياسة الأمان

## الإبلاغ عن ثغرة

لا تنشر مفاتيح أو بيانات شخصية أو تفاصيل استغلال في Issue عامة. استخدم **Private vulnerability reporting** من تبويب Security في GitHub، وأرفق:

- وصفًا مختصرًا للأثر المتوقع.
- خطوات إعادة إنتاج لا تتضمن بيانات حقيقية.
- الملفات أو المسارات المتأثرة.
- اقتراحًا للمعالجة إن توفر.

## قواعد بيانات الاعتماد

- لا تُحفظ مفاتيح API أو رموز الوصول في المستودع أو في متغيرات تبدأ بـ `VITE_` إذا كانت سرية؛ تلك المتغيرات تصبح جزءًا من حزمة المتصفح.
- أي تكامل حساس يجب أن يمر عبر خدمة خلفية تتحقق من الصلاحيات وتطبق تحديد المعدل والتسجيل الآمن.
- عند اكتشاف مفتاح منشور، يجب حذفه من النسخة الحالية **وتدويره من مزود الخدمة**؛ حذف النص من Git وحده لا يلغي المفتاح.
- شغّل `npm run security:secrets` قبل رفع أي تغيير.

## نطاق المنتج الحالي

البيانات المعروضة نموذجية وليست مصدرًا رسميًا حيًا. لا يوجد في النسخة الحالية تسجيل دخول حقيقي، ولا ينبغي تمثيل محاكاة محلية على أنها مصادقة آمنة.
